# Copilot Instructions

## Project Guidelines
- In COMET project: button1 (Send button) must be in the parent container of panelHistory (not inside panelHistory), so it's always visible and unaffected by smart button operations. Smart buttons go inside panelHistory and stack upward from the bottom.